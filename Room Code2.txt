using System;
using System.Collections.Generic;
using System.ComponentModel;
using System.Data;
using System.Data.SqlClient;
using System.Drawing;
using System.Text;
using System.Text.RegularExpressions;
using System.Windows.Forms;

namespace SoftwareEngineeringSP
{
    public partial class frmRoomInformation : Form
    {
        Connection contest = new Connection();
        SqlConnection sqlcon = new SqlConnection();
        SqlCommand cmd = new SqlCommand();
        public frmRoomInformation()
        {
            InitializeComponent();
            sqlcon = contest.getConnection();
        }

        private void btnHome_Click(object sender, EventArgs e)
        {
            this.Close();
        }

        private void btnSubmit_Click(object sender, EventArgs e)
        {
            //we have to pass this data that is submitted to the database 
            this.Close();
        }

        private void btnResetAll_Click(object sender, EventArgs e)
        {
            Action<Control.ControlCollection> func = null;
            func = (controls) =>
            {
                foreach (Control control in controls)
                {
                    if (control is TextBox)
                    {
                        (control as TextBox).Clear();
                    }
                    else
                    {
                        func(control.Controls);
                    }
                }
            };
            func(Controls);
        }

        private void tbxRoomIdentifier_TextChanged(object sender, EventArgs e)
        {
            String roomID = tbxRoomIdentifier.Text;
        }

        private bool SetRoomID(String roomID)
        {
            if (!Regex.Match(roomID, "^[A-Z][a-zA-Z][0-9]*$").Success)
            {
                return true;
            }
            else
                return false;
        }
        private void GetRoomID(String roomID)
        {
            //save roomID to database
        }

        private void tbxCapacity_TextChanged(object sender, EventArgs e)
        { 
            String capacity = tbxCapacity.Text;
        }

        private bool SetCapacity(String capacity)
        {
            try
            {
                if (Convert.ToInt32(capacity) > -1 && Convert.ToInt32(capacity) < 61)
                {
                    return true;
                    //find out what the smallest room capcity is
                }
                else
                {
                    return false;
                }
            }
            catch
            {
                return false;
            }
        }

        private void GetCapacity(String capacity)
        {
            //save Capacity to database
        }
    }
}
